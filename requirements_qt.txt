# Web framework and server
flask==2.3.3  # Core Flask framework for flask_server.py
gunicorn==23.0.0  # WSGI server for Unix-based production deployment
waitress==3.0.0  # WSGI server for Windows/cross-platform deployment
flask-cors==4.0.1  # Enables CORS for API requests from SwiftSaleGUI
flask-socketio==5.3.6  # WebSocket support for real-time updates
flask-limiter==3.5.0  # Rate limiting for Flask endpoints

# Payment processing
stripe==10.3.0  # Stripe SDK for checkout sessions and webhooks

# Database and networking
psycopg2-binary==2.9.9  # PostgreSQL adapter for CloudDatabaseManager
redis==5.0.8  # Redis client, likely for flask-limiter or flask-socketio
requests==2.32.3  # HTTP requests for API calls in SwiftSaleGUI
tenacity==8.5.0  # Retry logic for network/database operations
python-socketio==5.11.3  # Client-side SocketIO for SwiftSaleGUI

# GUI and image processing
PySide6==6.7.2  # Qt framework for SwiftSaleGUI
Pillow==10.4.0  # Image processing for logo in SwiftSaleGUI

# PDF generation
reportlab==4.2.2  # PDF creation for label annotation in SwiftSaleGUI

# Environment management
python-dotenv==1.0.1  # Load environment variables from .env files